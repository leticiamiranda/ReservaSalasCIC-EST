-# navigation styled for Bootstrap 3.0
.container
  %nav.navbar.navbar-primary
    = link_to image_tag("logo2.png"), root_path, class: 'navbar-brand', style: 'background-color:#ffffff'
    %ul.nav.navbar-nav.clearfix
      %li.nav-item
        = link_to "Reserva de Salas CIC/EST", root_path, class: 'h3', style: 'color:#19367a; background-color:#ffffff;'
      /- if user_signed_in?
      /  %ul.nav.navbar-nav.navbar-right  
      /    = button_to destroy_user_session_path, {:method => :delete, class: 'btn btn-danger'} do
      /      %span{:class => 'glyphicon glyphicon-off'}
      /      Sair
      /- else
      /  %ul.nav.navbar-nav.navbar-right  
      /    = button_to new_user_session_path, {:method => :get, class: 'btn btn-primary btn-primary', style: 'color:#ffffff'} do
      /      %span{:class => 'glyphicon glyphicon-user'}
      /      Login
  %hr
  %ul.nav.navbar-nav= render 'layouts/navigation_links'

  %nav.navbar.navbar-default
    .container-fluid
      / Brand and toggle get grouped for better mobile display
      .navbar-header
        %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{:href => "/reservas"} Home
      / Collect the nav links, forms, and other content for toggling
      #bs-example-navbar-collapse-1.collapse.navbar-collapse
        %ul.nav.navbar-nav
          %li
            %a{:href => "/salas"}
              Salas
              %span.sr-only (current)
          %li
            %a{:href => "/recursos"} Recursos
          
        - if user_signed_in?
          %ul.nav.navbar-nav.navbar-right  
            = button_to destroy_user_session_path, {:method => :delete, class: 'btn btn-danger', :style => 'margin-top: 0.2cm'} do
              %span{:class => 'glyphicon glyphicon-off'}
              Sair
        - else
          %ul.nav.navbar-nav.navbar-right  
            = button_to new_user_session_path, {:method => :get, class: 'btn btn-primary btn-primary', style: 'color:#ffffff; margin-top: 0.2cm' } do
              %span{:class => 'glyphicon glyphicon-user'}
              Login        
            
    / /.container-fluid
